telegram:
  enabled: true
  bot_token: null  # Will use TELEGRAM_BOT_TOKEN env var
  chat_id: null    # Will use TELEGRAM_CHAT_ID env var
  image_width: 1400
  image_height: 900
  raise_on_error: false  # Continue pipeline even if Telegram fails
  header_message: |
    📊 <b>SignalFlow Analysis Complete</b>
    
    🔍 Strategy: {strategy_name}
    📅 Period: {period}
    📈 Signals: {n_signals}
    💱 Pairs: {n_pairs}
    ⏰ Generated: {timestamp}

strategy_name: "SMA_Crossover_Baseline"